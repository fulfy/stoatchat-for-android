name: Android CI

on:
  push:
    branches: [ "dev" ]
  workflow_dispatch:

jobs:
  build:
    # if: ${{ false }} # at the moment this simply wastes our minutes, we will enable it when we have unit tests etc.

    runs-on: ubuntu-latest

    env:
      RVX_SENTRY_DSN: ${{ vars.RVX_SENTRY_DSN }}
      RVX_ANALYSIS_ENABLED: ${{ vars.RVX_ANALYSIS_ENABLED }}
      RVX_ANALYSIS_BASEURL: ${{ vars.RVX_ANALYSIS_BASEURL }}
      RVX_DEBUG_APP_NAME: ${{ vars.RVX_DEBUG_APP_NAME }}
      RVX_BUILD_FLAVOUR_ID: ${{ vars.RVX_BUILD_FLAVOUR_ID }}

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: 'recursive'
      - name: set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'oracle'
          cache: gradle


      - name: Setup Android SDK
        uses: android-actions/setup-android@v2

      - setup: Setup Deno
        uses: denoland/setup-deno@v2
        with:
          # setting `cache-hash` implies `cache: true` and will replace
          # the default cache-hash of `${{ hashFiles('**/deno.lock') }}`
          cache-hash: ${{ hashFiles('**/deno.json') }}

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Setup repo
        run: deno run -A scripts/download_deps.ts --yes && cp stoatbuild.properties.example stoatbuild.properties && cp sentry.properties.example sentry.properties && cp app/google-services.json.example app/google-services.json && git submodule update --init --recursive

      - name: Build debug with Gradle
        run: ./gradlew --no-daemon assembleDebug

      - name: Archive debug APK
        uses: actions/upload-artifact@v4
        with:
          name: debug-apk
          retention-days: 30
          path: |
            app/build/outputs/apk/debug/app-debug.apk
